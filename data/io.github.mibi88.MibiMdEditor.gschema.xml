<schemalist>
  <schema id="io.github.mibi88.MibiMdEditor" path="/io/github/mibi88/MibiMdEditor/">
    <key name="bg-grid" type="b">
      <default>true</default>
      <summary>Does the text editor has a grid background?</summary>
      <description>
        If the text editor has a grid background.
      </description>
    </key>

    <key name="lhighlight" type="b">
      <default>true</default>
      <summary>Is current line highlighted in the text editor?</summary>
      <description>
        If current line is highlighted in the text editor.
      </description>
    </key>

    <key name="auto-indent" type="b">
      <default>true</default>
      <summary>
        Does the text editor indents automatically the new line?
      </summary>
      <description>
        If the text editor indents automatically a new line.
      </description>
    </key>

    <key name="mono-font" type="b">
      <default>true</default>
      <summary>
        Does the text editor uses a monospaced font to display his content?
      </summary>
      <description>
        If the text editor uses a monospaced font instead of a proportional one.
      </description>
    </key>

    <key name="script-names" type="as">
      <default>[]</default>
      <summary>
        Names of the diffrent scripts to generate HTML.
      </summary>
      <description>
        The names of the diffrent scripts to generate HTML from other markup languages.
      </description>
    </key>

    <key name="script-files" type="as">
      <default>[]</default>
      <summary>
        Files of the diffrent scripts to generate HTML.
      </summary>
      <description>
        The files of the diffrent scripts to generate HTML from other markup languages.
      </description>
    </key>

    <key name="script-syntax-highlighting" type="as">
      <default>[]</default>
      <summary>
        Language name for the syntax highlighting for the markup languages parsed by the scripts to generate HTML.
      </summary>
      <description>
        Language name for the syntax highlighting for the markup languages parsed by the scripts to generate HTML.
      </description>
    </key>
  </schema>
</schemalist>

